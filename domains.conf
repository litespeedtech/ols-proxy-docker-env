# One additional domain per line:
# DOMAIN, BACKEND_IP, BACKEND_PORT, PROXY_SOCKET, PROXY_METHOD, HEADER_SET
# HEADER_SET is an optional single header operation and is only supported in context mode.
# second.example.com, backend-service, 8080, false, context
# With an optional header operation:
# second.example.com, backend-service, 8080, false, context, RequestHeader set Origin "https://www.example.com"
