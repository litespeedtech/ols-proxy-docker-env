# One additional domain per line:
# DOMAIN, BACKEND_IP, BACKEND_PORT, PROXY_SOCKET, PROXY_METHOD, HEADER_SET
# HEADER_SET defaults to "Header set" in context mode; rewrite mode ignores it with a warning.
# second.example.com, backend-service, 8080, false, rewrite
# Context example with a response header:
# second.example.com, backend-service, 8080, false, context, Cache-Control public, max-age=11111
